{
  "name": "{{ name }}",
  "description": "{{ description }}",
  "version": "{{ version }}",
  "manifest_version": 2,
  "permissions": [ "<all_urls>", "webRequest", "webRequestBlocking" ],
  "background": {
    "scripts": [ "authenticator.js" ]
  }
}
